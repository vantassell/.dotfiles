[user]
	name = Andrew Van Tassell
	email = vantassell@gmail.com
[color "diff"]
	meta = yellow bold
[core]
  editor = nvim

# use neovim for git diff viewing
[diff]
    tool = nvimdiff
[difftool]
    prompt = false
[difftool "nvimdiff"]
    cmd = nvim -d "$LOCAL" "$REMOTE"
